Environment of "Jupyter Notebook" is strongly recommended for this program.

Install necessary modules for accurate output like below,

!pip install whois
!pip install --upgrade python-whois
!pip install matplotlib
!pip install socket
!pip install ssl
